version: '3.5'
services:
 backend:
  image: ng-backend-chemita
  volumes:
   - c:\angular\chems\backend:/backend #windows setup
   #- /chems/backend:/backend #linux setup
  restart: always
  ports:
   - 3000:3000

